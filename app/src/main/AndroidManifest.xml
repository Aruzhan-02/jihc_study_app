<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jihc_study">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JIHC_STUDY">
        <activity android:name=".Schedule_Day"></activity>
        <activity android:name=".navigation.TeachersList" />
        <activity android:name=".navigation.Quiz_schedules" />
        <activity android:name=".navigation.Focus_timer" />
        <activity android:name=".navigation.Lesson_files" />
        <activity android:name=".navigation.Homeworks" />
        <activity android:name=".navigation.SchedulePage" />
        <activity android:name=".navigation.Dashboard" />
        <activity android:name=".navigation.ProfilePage" />
        <activity android:name=".authentication.RegistrationPage" />
        <activity android:name=".authentication.LoginPage" />
        <activity android:name=".authentication.SplashScrenn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>